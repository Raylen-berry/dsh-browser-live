# CI 与本地都用同一套安装口径。
#
# legacy-peer-deps=true 的理由：本插件的 peerDependencies 是**宿主 DSH 在运行时提供的**
# （@deepseek-ai/cordis、dsh-tools、react…），测试本身一个都不需要 —— 只有 tools/verify-bridge*、
# tools/verify-extension、tools/verify-launch、tools/verify-browsers、tools/verify-result-cap
# 需要测试用的 npm 'ws'。npm 7+ 默认**自动安装 peerDependencies**，于是 `npm install` 会去拉
# 一整套 @deepseek-ai/*（35 个包），把一个"零外部依赖"的插件仓库的 CI 装依赖步骤变成
# 依赖宿主发布状态的高风险动作（宿主包版本一变 CI 就红）。
# 打开这一项后 lockfile 里只剩真正声明的 devDependencies（本仓库就是 ws 一个），
# 测试可跑、口径可复现，且不影响宿主按 peerDependencies 提供的运行时依赖。
legacy-peer-deps=true

# 显式钉公网 registry：lockfile 里写的就是这个源，CI（GitHub runner）必须能取到。
# 本机 $USERPROFILE/.npmrc 指向的是 npmmirror 镜像镜像，不钉的话 lockfile 里会写镜像 URL，
# 换到公网 runner 上取包可能失败 —— 这是"本机能装、CI 装不上"的同类坑。
# 注：这一行只影响**安装测试依赖**这一步；npm test 本身仍然全程不出网。
registry=https://registry.npmjs.org/
